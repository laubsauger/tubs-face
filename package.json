{
  "name": "tubs-bot",
  "version": "1.0.0",
  "description": "Tubs Bot — Animated Chatbot Face Interface",
  "main": "bridge-server.js",
  "scripts": {
    "start": "node src/bridge-server.js",
    "kill": "pkill -f 'node src/bridge-server.js' && pkill -f 'transcription-service.py' || true",
    "restart": "npm run kill && sleep 1 && npm start",
    "dev": "node src/bridge-server.js"
  },
  "dependencies": {
    "fs-extra": "^11.3.3",
    "ws": "^8.16.0"
  }
}
